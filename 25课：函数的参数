# print(max())# max()函数不能空着，也不能仅有一个值，更不能用数字和字符串等无法比较的数进行比较

# print(max())
#
# *args可变参数，可以传入0个或任意参数，传入后帮你打包成一个元组，从元组中拿到一个个元素

def foo(*args,**kwargs):# **kwargs就可以输入关键字了
    print(args)
    print(kwargs)# 它会输出成字典，关键字成为键，数字成为值
# foo()
# foo(1)
# foo(1,2)
foo(1,2,a=3,b=4)# 无法接收关键字参数
# foo(1,2,3,4,5,6,7)
# foo([1,2,3,4,5,6,7])
# foo({1,2,3,4},[5,6,7])
